# Files in this directory (if any) are packaged into a ConfigMap when configOverrides.enabled=true.
# Use the helper script to sync repository config/overrides here:
#   ./scripts/deployment/helm/atlas/scripts/populate-config-overrides.sh <release> <namespace> <path>
