apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas.svc.auth" . }}
  labels:
    {{- include "atlas.labels" . | nindent 4 }}
    component: auth-app
spec:
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: auth-app
  ports:
    - name: http
      port: {{ .Values.service.authApp.port }}
      targetPort: http
  type: ClusterIP
